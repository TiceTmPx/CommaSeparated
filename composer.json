{
    "name": "ticetmp/comma-separated",
    "description": "This function could be useful in scenarios where traditional Eloquent relationships are not applicable, and you need to manually handle the relationship using raw SQL queries with a comma-separated values approach",
    "type": "library",
    "autoload": {
        "psr-4": {
            "TiceTmP\\CommaSeparated\\": "src/"
        }
    },
    "authors": [
        {
            "name": "TiceTmP"
        }
    ],
    "require": {
        "ticetmp/comma-separated" : "1.0",
        "composer/installers" : "^1.0",
        "laravel/framework": "^5.6",
        "php": "^7.0"
    },
    "repositories" : [
        {
            "type": "vcs",
            "url": "https://github.com/TiceTmPx/CommaSeparated/tree/1.0"
        }
    ]
}
